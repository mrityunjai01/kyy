
def cry(a, b, c):
    """creates something taht cry so i want to do something

    Args:
        a (int): the first int
        b (int): second int
        c (int): third int
    """
    print(a, b, c)

class Baby:
    """Baby class that represents nn
    """
    def __init__(self, name):
        """Initializes Baby

        Args:
            name (string): the name of baby
        """
        self.name = name
    
    def can_i_cry(self, j, k):
        """Tells if i can cry

        Args:
            j (int): the first int
            k (string): the string  

        Returns:
            bool: True if i can cry
        """
        while True:
            print(j)
        return j
    